import { useParams } from 'next/navigation'
import React from 'react'

const OpeningPractice = () => {
    const {id} = useParams()
  return (
    <div>OpeningPractice</div>
  )
}

export default OpeningPractice